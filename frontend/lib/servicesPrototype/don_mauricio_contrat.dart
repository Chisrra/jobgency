import 'package:flutter/material.dart';

class ContratarDonMauricio extends StatelessWidget {
  const ContratarDonMauricio({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Proceso"),
      ),
    );
  }
}
